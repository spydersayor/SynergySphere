# Prefer explicit settings over DATABASE_URL for local dev
PORT=8000

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=synergysphere

# Optional: if you want to override all above in one string
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/synergysphere
